// Global configuration to force dynamic rendering for all routes
// This prevents SSR issues with framer-motion and other client-side libraries
export const dynamic = "force-dynamic";
export const revalidate = 0;
